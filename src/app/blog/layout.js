export const metadata = {
  title: "Blog & Technical Insights | Privia Solutions",
  description: "Deep dive technical articles, autoscale checklists, zero-trust infrastructure guidelines, and SRE resources written by our developers.",
};

export default function BlogLayout({ children }) {
  return children;
}
