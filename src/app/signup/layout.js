export const metadata = {
  title: "Sign Up | Aetheris Systems",
  description: "Create an Aetheris account to set up cloud budgets, monitor clusters, and manage your custom software projects.",
};

export default function SignupLayout({ children }) {
  return children;
}
