export const metadata = {
  title: "Log In | Aetheris Systems",
  description: "Log in to your client control panel to coordinate system setups, audit pipeline rules, and monitor custom software modules.",
};

export default function LoginLayout({ children }) {
  return children;
}
