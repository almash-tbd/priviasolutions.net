export const metadata = {
  title: "Frequently Asked Questions | Aetheris Systems",
  description: "Find answers about custom software engineering paths, cloud database optimizations, security audits, and systems support SLA schedules.",
};

export default function FAQLayout({ children }) {
  return children;
}
