export const metadata = {
  title: "QA Automation & Performance | Aetheris Systems",
  description: "Automated regression testing, heavy load simulation, and system bottleneck checks integrated directly into pipeline builds.",
};

export default function QAPerformanceLayout({ children }) {
  return children;
}
